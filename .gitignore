node_modules/
dist/
coverage/
.DS_Store
*.local
.env
.env.*
!.env.example

# Local Blender and generated city artifacts are intentionally not committed.
*.blend
*.blend1
data/raw/
data/generated/
# The worktree-local `data/generated` is a SYMLINK into the main checkout, and a
# trailing-slash pattern matches only directories, so it needs its own entry.
/data/generated
public/tiles/
public/data/
artifacts/offline-ingest/
__pycache__/
*.pyc
artifacts/blender/
artifacts/block835-reference-determinism/
# Raw T009 canary validation evidence. The hashes are committed in
# data/block835-canary-validation-20260811/evidence-inventory.json so they stay
# checkable after this worktree-local tree is removed.
artifacts/block835-canary-validation-20260811/
# T013 midtown-core canary work root: full-wave censuses, renders, and journey
# evidence. The hashes are committed in
# data/midtown-core-20260811/evidence-inventory.json so they stay checkable
# after this worktree-local tree is removed.
artifacts/midtown-core-20260811/

# T025 V3 determinism scratch
artifacts/block835-v3-determinism/

# T027 V3T textured-package determinism scratch. The hashes that matter are
# committed in data/manhattan-esb-block-reference-20260811-v3t/.
artifacts/block835-v3t-determinism/

# T026 P3 midtown-core V3 work root: full-wave censuses, Blender renders, and
# journey evidence. The hashes that matter are committed in
# data/midtown-core-20260811-v3/.
artifacts/midtown-core-20260811-v3/

# T028 Cesium sampler-aliasing scratch root: the two sampler VARIANTS of the
# private V3T package and the captured stills. The checksums, the camera
# stations and the written verdict are committed in
# data/manhattan-esb-block-reference-20260811-v3t/cesium-sampler-evidence.json
# so the evidence stays checkable after this worktree-local tree is removed.
artifacts/block835-v3t-cesium-20260811/

# T015 Lower-Manhattan canary work root: the full-wave census over all 6,425
# owned buildings, the Blender sample inputs and renders, and the stage
# receipts. The hashes that matter are committed in
# data/lower-manhattan-20260812/.
artifacts/lower-manhattan-20260812/

# T015 kill-switch scratch root: the untextured baseline and textured candidate
# variants of the heaviest renderable cell, plus the captured stills. The
# measurements, the camera stations, every still's checksum and the written
# verdict are committed in data/lower-manhattan-20260812/kill-switch-evidence.json
# and kill-switch-verdict.json so the evidence stays checkable after this
# worktree-local tree is removed.
artifacts/lower-manhattan-20260812-probe/

# T016 Lower-Manhattan P1 promotion work root: the same full-wave census, the
# curated subset's Blender sample inputs and renders, and the stage receipts.
# The hashes that matter are committed in data/lower-manhattan-20260812-p1/.
artifacts/lower-manhattan-20260812-p1/

# T016 acceptance-measurement scratch root: the uncapped CDP frame-time and
# heap captures and the station stills. The measurements, the camera stations
# and every still's checksum are committed in
# data/lower-manhattan-20260812-p1/acceptance-evidence.json.
artifacts/lower-manhattan-20260812-p1-acceptance/

# T017 Southern-remainder canary work root: the full-wave census over all 9,603
# owned buildings, the Blender sample inputs and renders, and the stage
# receipts. The hashes that matter are committed in
# data/southern-remainder-20260812/.
artifacts/southern-remainder-20260812/

# T017 renderer-journey scratch root: the captured stills. Every still's
# checksum, the URL it was taken at and the DOM text each journey read are
# committed in data/southern-remainder-20260812/journey-evidence.json so the
# evidence stays checkable after this worktree-local tree is removed.
artifacts/southern-remainder-20260812-journeys/

# T018 Southern-remainder P1 promotion work root: the same full-wave census, the
# curated subset's Blender sample inputs and renders, and the stage receipts.
# The hashes that matter are committed in data/southern-remainder-20260812-p1/,
# including the skyline census the curation's optimality claim is enumerated
# over.
artifacts/southern-remainder-20260812-p1/

# T018 acceptance-measurement scratch root: the uncapped CDP frame-time and heap
# captures and the station stills, taken at the RAISED cache cap. The
# measurements, the camera stations and every still's checksum are committed in
# data/southern-remainder-20260812-p1/acceptance-evidence.json.
artifacts/southern-remainder-20260812-p1-acceptance/

# T018 renderer-journey scratch root: the captured stills. Every still's
# checksum, the URL it was taken at and the DOM text each journey read are
# committed in data/southern-remainder-20260812-p1/journey-evidence.json.
artifacts/southern-remainder-20260812-p1-journeys/

# T019 Central-and-upper-Manhattan canary work root: the full-wave census over
# all 11,721 owned buildings — the largest wave of the six — the Blender sample
# inputs and renders, and the stage receipts. The hashes that matter are
# committed in data/central-upper-manhattan-20260812/.
artifacts/central-upper-manhattan-20260812/

# T019 renderer-journey scratch root: the captured stills. Every still's
# checksum, the URL it was taken at and the DOM text each journey read are
# committed in data/central-upper-manhattan-20260812/journey-evidence.json so the
# evidence stays checkable after this worktree-local tree is removed.
artifacts/central-upper-manhattan-20260812-journeys/

# T020 Central-and-upper-Manhattan P1 promotion work root: the same full-wave
# census over all 11,721 owned buildings, the curated subset's Blender sample
# inputs and renders, and the stage receipts. The hashes that matter are
# committed in data/central-upper-manhattan-20260812-p1/, including the skyline
# census the curation's optimality claim is enumerated over.
artifacts/central-upper-manhattan-20260812-p1/

# T020 acceptance-measurement scratch root: the uncapped CDP frame-time and heap
# captures and the station stills, taken on the FIVE-wave composition. The
# measurements, the camera stations and every still's checksum are committed in
# data/central-upper-manhattan-20260812-p1/acceptance-evidence.json.
artifacts/central-upper-manhattan-20260812-p1-acceptance/

# T020 renderer-journey scratch root: the captured stills. Every still's
# checksum, the URL it was taken at and the DOM text each journey read are
# committed in data/central-upper-manhattan-20260812-p1/journey-evidence.json.
artifacts/central-upper-manhattan-20260812-p1-journeys/

# T021 Northern-Manhattan canary work root: the full-wave census over all 10,230
# owned buildings — the LAST wave the committed ledger declares — the Blender
# sample inputs and renders, and the stage receipts. The hashes that matter are
# committed in data/northern-manhattan-20260812/.
artifacts/northern-manhattan-20260812/

# T021 renderer-journey scratch root: the captured stills. Every still's
# checksum, the URL it was taken at and the DOM text each journey read are
# committed in data/northern-manhattan-20260812/journey-evidence.json so the
# evidence stays checkable after this worktree-local tree is removed.
artifacts/northern-manhattan-20260812-journeys/

# T022 Northern-Manhattan P1 promotion work root: the same full-wave census over
# all 10,230 owned buildings, the curated subset's Blender sample inputs and
# renders, and the stage receipts. The hashes that matter are committed in
# data/northern-manhattan-20260812-p1/, including the skyline census the
# curation's optimality claim is enumerated over.
artifacts/northern-manhattan-20260812-p1/

# T022 acceptance-measurement scratch root: the uncapped CDP frame-time and heap
# captures and the station stills, taken on the SIX-wave composition. The
# measurements, the camera stations and every still's checksum are committed in
# data/northern-manhattan-20260812-p1/acceptance-evidence.json.
artifacts/northern-manhattan-20260812-p1-acceptance/

# T022 renderer-journey scratch root: the captured stills. Every still's
# checksum, the URL it was taken at and the DOM text each journey read are
# committed in data/northern-manhattan-20260812-p1/journey-evidence.json.
artifacts/northern-manhattan-20260812-p1-journeys/

# T023 public-showcase smoke work root: the captured stills. Every still's
# checksum, the URL it was taken at and the readings each journey made are
# committed in data/public-showcase-20260812/smoke-evidence.json so the
# evidence stays checkable after this worktree-local tree is removed.
artifacts/public-showcase-20260812/

# T029 bounded-gap closure work root: the 1440p facade stills, the keyboard and
# reduced-motion captures, the six-wave heap/concurrency still and the mobile
# journey stills. Every still's checksum, the viewport it was taken at, the URL
# and the DOM readings each capture made are committed in
# data/goal-bounded-gaps-20260812/ so the evidence stays checkable after this
# worktree-local tree is removed.
artifacts/goal-bounded-gaps-20260812/

# T001 citywide overview-tier census work root: stage receipts only. Every
# measurement the decision rests on is committed under
# data/citywide-overview-census-20260814/ with its own checksum sidecar, so the
# evidence stays checkable after this worktree-local tree is removed. The census
# is census-only and retains no geometry, so there is nothing else here.
artifacts/citywide-overview-census-20260814/

# T004 Blender-agreement work root: the per-sample analytic inputs, the
# selection table and the raw Blender inspection report. Every number the
# agreement rests on — the sample list, the deterministic selection rule, each
# building's measured deltas and each measured file's checksum — is committed in
# data/mass-generation-20260816/blender-agreement.json with its own sidecar, so
# the evidence stays checkable after this worktree-local tree is removed. No
# geometry and no render is retained here.
artifacts/mass-generation-20260816/
